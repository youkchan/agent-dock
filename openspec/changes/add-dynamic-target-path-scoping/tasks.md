## 1. 実装タスク
- [ ] 1.1 自動スコープ用フィールドをタスク状態へ追加する
  - 成果物: `effective_target_paths` とスコープ更新履歴を保持
- [ ] 1.2 タスク開始時の初期スキャンを実装する
  - 成果物: `target_paths=*` タスクの初動スキャンと自動絞り込み
- [ ] 1.3 実行中スコープ拡張フローを実装する
  - 成果物: 拡張要求イベント、承認後の `target_paths` 更新
- [ ] 1.4 スコープ境界を `TARGET_PROJECT_DIR` 配下に強制する
  - 成果物: ルート外パス拒否と正規化
- [ ] 1.5 スコープ更新の監査ログ出力を実装する
  - 成果物: `who/when/reason/paths` の永続化
- [ ] 1.6 テストを追加する
  - 成果物: 初期絞り込み、拡張成功、ルート外拒否、監査ログのテスト
- [ ] 1.7 README を更新する
  - 成果物: `*` 運用時の自動絞り込み・拡張フロー説明

## 2. 検証項目
- [ ] `python -m unittest tests/test_orchestrator.py -v` が通る
- [ ] `target_paths=*` タスク開始時に `effective_target_paths` が生成される
- [ ] 実行中に必要ファイル発見時、拡張要求でスコープ更新できる
- [ ] `TARGET_PROJECT_DIR` 外のパスは拡張要求で拒否される
- [ ] スコープ更新履歴が state に保存される
