{
  "name": "agent-dock-local-dev",
  "version": "0.0.0",
  "private": true,
  "description": "Local npm manifest for linking agent-dock during development",
  "scripts": {
    "agent-dock:help": "./node_modules/.bin/agent-dock --help"
  }
}
